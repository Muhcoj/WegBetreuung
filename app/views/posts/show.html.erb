<div style="width: 80%; margin: auto; text-align: center;">
    <div style="padding: 25px;">
      <h3 style="color: #337ab7;">
      <%= @post.title %>
      </h3>
      <hr>
      <p style="font-size: 0.8em; color: #777777; "><i><%= @post.date %></i></p>
      <p>
        <%= @post.content %>
      </p>
      <%= link_to 'Bearbeiten', edit_post_path(@post), id: "edit_#{@post.id}" %>
      <%= link_to 'Löschen', post_path(@post), method: :delete, id: "delete_post_#{@post.id}_from_index", data: { confirm: 'Sind Sie sicher?' } %>
    </div>
  </div>