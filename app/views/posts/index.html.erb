

<div id="posts">
  <%= render @posts %>
</div>

<div id="paginator" class="col-md-12">
  <%= paginate @posts, remote: true %>
</div>

<% @users = User.all %>
<% @users.each do |user| %>
  <%= user.email %><br/>
  <%= user.full_name %><br/>
<% end %>